mixin breadcrumb(...items)
  nav(aria-label="breadcrumb" style="--bs-breadcrumb-divider: '>';" class="nav__breadcrumb")
    ol(class="breadcrumb")
      each item in items
        li(class="breadcrumb-item" aria-current="page")=item
